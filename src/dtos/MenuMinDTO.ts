import { WorkingDayDTO } from "./WorkingDayDTO";

export type MenuMinDTO = {
    id: number;
    name: string;
    description: string;
    imageUri: string;
    workingDay: String;
    productCount: number;
  };
  